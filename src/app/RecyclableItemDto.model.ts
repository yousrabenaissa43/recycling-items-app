export interface RecyclableItemDto {
    itemName: string;
    materialType: string;
    location?: string; // Optional if you're not using it for adding
    recycledDate?: string; // Optional if you're not using it for adding
  }
  